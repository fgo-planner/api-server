import { GameCharacter } from 'data/types';

/**
 * A non-servant NPC enemy, including non-playable servant enemies that do not
 * have an ID number.
 */
export type GameEnemy = GameCharacter & {
    
}